#!/usr/bin/env bash
# -*- mode: sh; sh-shell: bash; -*-
#MISE description="Resume WSJT-X CQ automatically after each logged QSO"
#MISE raw=true

set -euo pipefail

exec scripts/radio/wsjtx-resume-cq.sh "$@"
