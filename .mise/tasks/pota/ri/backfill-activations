#!/usr/bin/env bash
# -*- mode: sh; sh-shell: bash; -*-
#MISE description="Backfill cached POTA activation history for Rhode Island references"

set -euo pipefail

node --experimental-strip-types scripts/pota/ri-tracker.mjs backfill-activations
