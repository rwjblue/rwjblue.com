#!/usr/bin/env bash
# -*- mode: sh; sh-shell: bash; -*-
#MISE description="Refresh the cached N1RWJ POTA profile and merge recent activity"

set -euo pipefail

node --experimental-strip-types scripts/pota/ri-tracker.mjs update-profile
