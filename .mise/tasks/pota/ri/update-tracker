#!/usr/bin/env bash
# -*- mode: sh; sh-shell: bash; -*-
#MISE description="Refresh low-impact Rhode Island POTA tracker data"

set -euo pipefail

node --experimental-strip-types scripts/pota/ri-tracker.mjs update-tracker
